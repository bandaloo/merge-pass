/*
import { AllVals } from "../exprtypes";
import { Operator as Op } from "./expr";

export class CountExpr<T extends AllVals, U extends AllVals> extends Op<T> {
  constructor() {
    super(tag`(uCount == 0 ? ${} : ${})`);
  }
}
*/
